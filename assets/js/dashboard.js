/**
 * Dashboard JavaScript
 */

import { api, showToast } from './app.js';

// Dashboard is mostly server-rendered, but we handle logout here

document.addEventListener('DOMContentLoaded', () => {
    // Logout form handled in app.js
});
